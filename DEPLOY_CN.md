# 🇨🇳 国内免费部署方案指南

由于 Vercel 在国内访问不稳定，以下是几个国内可用的免费部署方案。

## 📋 方案对比

| 方案 | 免费额度 | 国内访问 | 备案要求 | 配置难度 | 推荐度 |
|------|---------|---------|---------|---------|--------|
| Gitee Pages | ✅ 完全免费 | ⭐⭐⭐⭐⭐ | 需要 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 腾讯云静态网站托管 | ✅ 免费 | ⭐⭐⭐⭐⭐ | 需要 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 腾讯云开发 CloudBase | ✅ 免费额度 | ⭐⭐⭐⭐⭐ | 不需要 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 阿里云 OSS + CDN | ✅ 免费额度 | ⭐⭐⭐⭐⭐ | 需要 | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Cloudflare Pages | ✅ 完全免费 | ⭐⭐⭐ | 不需要 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🎯 方案一：Gitee Pages（最推荐）

### 优点
- ✅ 完全免费
- ✅ 国内访问速度快
- ✅ 配置简单，类似 GitHub Pages
- ✅ 支持自动部署
- ✅ 提供 `用户名.gitee.io` 免费域名

### 缺点
- ⚠️ 需要 ICP 备案（个人网站备案较简单）
- ⚠️ 不支持 Serverless Functions（需要单独配置后端）

### 部署步骤

#### 1. 创建 Gitee 仓库

1. 访问 https://gitee.com 注册账号
2. 点击右上角 **+** → **新建仓库**
3. 仓库名称：`wedding-invitation`
4. 选择 **公开**
5. 点击 **创建**

#### 2. 推送代码到 Gitee

```bash
cd E:\WebProject\wedding

# 添加 Gitee 远程仓库（如果还没有）
git remote add gitee https://gitee.com/你的用户名/wedding-invitation.git

# 或者如果已有 GitHub，添加第二个远程
git remote set-url --add --push origin https://gitee.com/你的用户名/wedding-invitation.git

# 推送代码
git push gitee master
```

#### 3. 启用 Gitee Pages

1. 进入仓库页面
2. 点击 **服务** → **Gitee Pages**
3. 选择部署分支：`master`
4. 选择部署目录：`dist`（构建后的目录）
5. 点击 **启动**
6. 等待部署完成（约 1-2 分钟）

#### 4. 访问网站

部署完成后，访问地址：
```
https://你的用户名.gitee.io/wedding-invitation/
```

#### 5. 自动部署配置

创建 `.gitee-pages.yml` 文件（可选，用于自动构建）：

```yaml
build:
  - npm install
  - npm run build
publish:
  - dist
```

---

## 🎯 方案二：腾讯云静态网站托管（推荐）

### 优点
- ✅ 免费额度充足（50GB 存储，10GB 流量/月）
- ✅ 国内访问速度快
- ✅ 支持自定义域名
- ✅ 支持 HTTPS
- ✅ 与腾讯云其他服务集成方便

### 缺点
- ⚠️ 需要 ICP 备案（如果使用自定义域名）
- ⚠️ 需要腾讯云账号

### 部署步骤

#### 1. 注册腾讯云账号

1. 访问 https://cloud.tencent.com
2. 注册/登录账号
3. 完成实名认证

#### 2. 开通静态网站托管

1. 进入 **云开发** → **静态网站托管**
2. 创建环境（选择免费套餐）
3. 等待环境创建完成

#### 3. 构建项目

```bash
cd E:\WebProject\wedding
npm install
npm run build
```

#### 4. 上传文件

1. 在静态网站托管控制台，点击 **上传文件**
2. 选择 `dist` 目录下的所有文件
3. 上传完成后，点击 **一键部署**

#### 5. 访问网站

部署完成后会提供访问地址：
```
https://你的环境ID.cloudbaseapp.com
```

#### 6. 配置自定义域名（可选）

1. 在静态网站托管中点击 **域名管理**
2. 添加自定义域名
3. 配置 DNS 解析
4. 等待审核（需要备案）

---

## 🎯 方案三：腾讯云开发 CloudBase（最佳，支持后端）

### 优点
- ✅ 免费额度：1GB 数据库，2GB 存储，5GB 流量/月
- ✅ 国内访问速度快
- ✅ **支持云函数（Serverless Functions）**
- ✅ **支持云数据库**
- ✅ 不需要备案（使用默认域名）
- ✅ 可以存储留言数据

### 缺点
- ⚠️ 需要学习云函数配置

### 部署步骤

#### 1. 安装 CloudBase CLI

```bash
npm install -g @cloudbase/cli
```

#### 2. 登录腾讯云

```bash
tcb login
```

#### 3. 初始化项目

```bash
cd E:\WebProject\wedding
tcb init
```

选择：
- 环境：创建新环境或选择已有环境
- 框架：静态网站托管
- 云函数：是（用于留言功能）

#### 4. 创建云函数（用于留言 API）

创建 `cloudfunctions/messages/index.js`：

```javascript
const cloud = require('@cloudbase/node-sdk')

const app = cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = app.database()

exports.main = async (event, context) => {
  const { method, data } = event

  try {
    if (method === 'GET') {
      // 获取留言
      const result = await db.collection('messages')
        .orderBy('createdAt', 'desc')
        .get()
      return {
        code: 0,
        data: result.data
      }
    }

    if (method === 'POST') {
      // 创建留言
      const result = await db.collection('messages').add({
        name: data.name,
        content: data.content,
        createdAt: new Date()
      })
      return {
        code: 0,
        data: result
      }
    }

    return { code: -1, message: '方法不支持' }
  } catch (error) {
    return { code: -1, message: error.message }
  }
}
```

#### 5. 部署

```bash
# 部署静态网站
tcb hosting deploy dist

# 部署云函数
tcb fn deploy messages
```

#### 6. 访问网站

部署完成后会提供访问地址。

---

## 🎯 方案四：阿里云 OSS + CDN

### 优点
- ✅ 免费额度：40GB 存储，5GB 流量/月
- ✅ 国内访问速度快
- ✅ 支持自定义域名

### 缺点
- ⚠️ 需要备案（使用自定义域名）
- ⚠️ 配置相对复杂

### 部署步骤

#### 1. 开通 OSS

1. 访问 https://oss.console.aliyun.com
2. 创建 Bucket
3. 设置权限为 **公共读**

#### 2. 上传文件

```bash
# 安装 OSS 工具
npm install -g ossutil

# 配置
ossutil config

# 上传文件
ossutil cp -r dist/ oss://你的bucket名称/ --update
```

#### 3. 配置静态网站托管

1. 在 OSS 控制台，点击 **基础设置** → **静态网站托管**
2. 设置默认首页：`index.html`
3. 设置错误页：`index.html`（用于 SPA 路由）

#### 4. 访问网站

OSS 会提供访问地址：
```
https://你的bucket名称.oss-cn-区域.aliyuncs.com
```

---

## 🎯 方案五：Cloudflare Pages（备选）

### 优点
- ✅ 完全免费
- ✅ 全球 CDN
- ✅ 配置简单
- ✅ 不需要备案

### 缺点
- ⚠️ 国内访问可能不稳定（部分地区）

### 部署步骤

#### 1. 注册 Cloudflare 账号

1. 访问 https://pages.cloudflare.com
2. 注册/登录账号

#### 2. 连接 GitHub 仓库

1. 点击 **Create a project**
2. 选择 **Connect to Git**
3. 授权 GitHub，选择仓库

#### 3. 配置构建

- Build command: `npm run build`
- Build output directory: `dist`
- Root directory: `/`

#### 4. 部署

点击 **Save and Deploy**，等待部署完成。

访问地址：
```
https://你的项目名.pages.dev
```

---

## 📝 通用配置说明

### 1. 修改 base 路径（如果部署在子目录）

如果部署在 `gitee.io/仓库名/` 这样的子路径，需要修改 `vite.config.js`：

```javascript
export default defineConfig({
  base: '/wedding-invitation/', // 改为你的仓库名
  // ... 其他配置
})
```

### 2. 环境变量配置

创建 `.env.production` 文件：

```env
VITE_API_URL=https://你的API地址
```

### 3. 构建项目

所有方案都需要先构建：

```bash
npm install
npm run build
```

构建后的文件在 `dist` 目录。

---

## 🔐 后端 API 方案（用于留言功能）

### 方案 A：使用腾讯云开发（推荐）

- 免费额度充足
- 国内访问稳定
- 支持云函数和数据库
- 配置相对简单

### 方案 B：使用 LeanCloud

- 免费额度：3GB 存储，5GB 流量/月
- 国内访问稳定
- 提供 REST API
- 配置简单

### 方案 C：使用 Bmob

- 免费额度：10,000 次 API 调用/天
- 国内访问稳定
- 提供 REST API

---

## ✅ 推荐方案总结

### 如果只需要静态网站：
**Gitee Pages** - 最简单，完全免费

### 如果需要后端 API（留言功能）：
**腾讯云开发 CloudBase** - 免费额度充足，支持云函数和数据库

### 如果需要自定义域名：
**腾讯云静态网站托管** 或 **阿里云 OSS**

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看各平台的官方文档
2. 联系平台客服
3. 查看项目 README.md

